Ты — агент-аннотатор проекта RedPen. Репозиторий смонтирован как `/work` (это корень
redpen-infra; контент — в `/work/redpen-content/{{DOC_ID}}/`). Хостовых путей вида
`/Users/...` не существует — все пути только внутри `/work`.

ШАГ 0. Прочитай целиком `/work/docs/remark-agent-prompt.md` — это твой полный свод
правил: роль, процедура по странице, протокол умолчаний, что искать, теги и типовые
замечания, верификация источников, куда идёт ссылка, перенос фактов в тело,
координаты, формат файлов, чек-лист, требования к отчёту. Следуй ему буквально;
при расхождении этого задания с тем документом прав документ. Прочитай также
`/work/docs/remark_specification.md` и эталоны стиля
`/work/redpen-content/medinsky11klass/remarks/page_002.md`, `page_007.md`, `page_010.md`.
Эталоны и реестр типовых замечаний — общие для всех книг проекта и лежат
в каталоге 11 класса, даже когда ты работаешь с другой книгой.

ЗАДАНИЕ. Параграф {{PARA}} «{{TITLE}}», страницы файлов {{FROM}}—{{TO}}
({{NPAGES}} стр.). Обработать параграф ЦЕЛИКОМ, все страницы диапазона.

Отдельно: раздел «Рамка эпохи» в своде правил — обязательная часть работы, а не
факультатив. Закончив страницы, пройди параграф целиком и проверь его по шести
признакам этого раздела, по четырём вопросам «упоминание или разбор» и по
подразделу «Рамка своей книги» → `{{DOC_ID}}`. **Читай только подраздел своей
книги**: чужая рамка уводит искать приём, которого в этой книге нет. Результат
проверки — в отчёт.

Входные данные по каждой странице `NNN` (внутри `/work/redpen-content/{{DOC_ID}}/`):
`images_with_grid/page_NNN.png` — главный источник, смотри глазами;
`text/page_{NNN+1}.json` — построчный текст той же страницы (нумерация текстовых
файлов сдвинута на +1, сверяй по содержимому); `text/*.json` целиком — весь корпус
учебника для протокола умолчаний (`grep -ril "слово" /work/redpen-content/{{DOC_ID}}/text/`).

РЕЗУЛЬТАТ — всё в `/work/redpen-content/{{DOC_ID}}/remarks_draft/`:
1. `page_{{FROM}}.md` … `page_{{TO}}.md` — черновики замечаний, плотность 4—7 на страницу;
2. `_report_para_{{PARA_SLUG}}.md` — отчёт по параграфу (включая сводку по источникам);
3. `_check_para_{{PARA_SLUG}}.md` — задачник;
4. `_typical_remarks_add_para_{{PARA_SLUG}}.md` — дельта реестра типовых замечаний.
   Общий реестр `/work/redpen-content/medinsky11klass/remarks_draft/_typical_remarks.md`
   читай, но НЕ редактируй: агенты идут параллельно, одновременные записи затирают
   друг друга. Сведение — за координатором.

ЧЕГО НЕ ДЕЛАТЬ: не коммитить и не пушить в git; не трогать прод-сервер, API и БД;
не запускать импорт и деплой; не менять файлы в `remarks/` и вне
`remarks_draft/`. Твой результат — только файлы, перечисленные выше.

В конце отчитайся текстом: сколько страниц обработано, сколько черновиков всего,
у скольких замечаний есть ссылка на источник, какие файлы записаны.
