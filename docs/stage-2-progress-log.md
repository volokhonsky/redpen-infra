# Прогресс: Этап 2 + адресация страниц (docs/agent-instructions-stage-2.md)

Журнал ведётся по ходу работы агента, чтобы можно было продолжить после
остановки (usage limit). Каждый пункт — коммит на `main` в локальном репо.
Деплой на прод — только после подтверждения пользователем (см. runbook в
исходном плане).

Статусы: `[ ]` не начато, `[~]` в процессе, `[x]` готово (закоммичено, тесты зелёные).

## Часть A — SQLite + публикация

- [x] A2.1 Схема БД: annotations + annotation_history (db.py) — коммит 0071460
- [x] A2.2 Публикатор scripts/api/publisher.py — коммит 1d540ef
- [x] A2.3 Импорт scripts/api/import_annotations.py — коммит e8afd25
- [x] A2.4 Перевод editor-эндпоинтов на БД (main.py, удаление storage.py page-функций) — коммит 93e0c05
- [x] A2.5 POST /api/admin/publish-all + publish_all() на старте — коммит 6cd3b94
- [x] A2.6 content-sync: exclude annotations + chown — коммит 7c28f3d (патч и entrypoint.sh — там был дублирующий bash-rsync)
- [x] A2.7 export_annotations.py + флаг --annotations-from-md в build_website.py — коммит 2338125
- [x] A2.8 Документация и конфиги (docker-compose, .env.sample, README, STATE_OVERVIEW) — коммит 1a485e2

**Часть A завершена локально (не задеплоена).** Все 134 pytest теста зелёные.

## Часть B — адресация страниц

- [x] B.1 Фикс pageKey в редакторе (main.js / redpen-editor-bootstrap.js) — коммит 9f6b1be (визуально в браузере не проверял: сейчас physicalStart=1 везде, поведение не меняется; JS-синтаксис проверен node --check)
- [x] B.2 Генератор манифеста scripts/generate_page_manifest.py — коммит f9c2503
- [x] B.3 Просмотрщик: манифест + ?p= — коммит 2ca5475 (проверено вручную в браузере на 2 синтетических сборках: манифест-режим и легаси-режим)
- [x] B.4 API: _validate_page_key вместо _validate_page_num — коммит 543b75c
- [x] B.5 Мелочи (корневой metadata.json, доки) — коммиты 551a799 (redpen-publish, локально, не запушен), 4acb4bb

**Часть B завершена локально (не задеплоена).** Все 161 pytest тестов
зелёные. B.3 (JS-логика просмотрщика) дополнительно проверена вручную в
браузере на двух синтетических сборках (см. коммит 2ca5475) — playwright-тесты
для неё сознательно не писались (см. заметку ниже).

## Деплой

- [ ] Показать пользователю готовое локально состояние, дождаться подтверждения
- [ ] Runbook A выполнен на проде
- [ ] Runbook B выполнен на проде

---

## Заметки по ходу работы

- Части A и B полностью реализованы и закоммичены локально на `main`
  (13 коммитов, см. номера выше). `pytest` — 161 тест, все зелёные.
- Не сделано умышленно / вне рамок:
  - Playwright-тесты для B.3 (манифест + `?p=`) — заменены ручной проверкой
    в браузере (2 синтетические сборки: манифест-режим и легаси-режим,
    подробности в коммите `2ca5475`). При желании можно дописать позже по
    образцу `tests/annotation_position_tests.py`.
  - Манифест для `medinsky11klass` не сгенерирован: нужна секция
    `pageNumbering.printedStartFile/printedStartNumber` в его `meta.json` —
    это решение о данных (какие файлы считать обложкой/форзацем), не
    техническая задача. Без него документ остаётся в легаси-режиме — ничего
    не ломается.
  - Оглавление по `chapters` в UI — опциональный бонус, не делался.
  - `TODO` (корневой файл, пункт про нумерацию A1/A2/1/2/3) — не отмечен
    выполненным, как и просили («после деплоя»).
- В `redpen-publish` (отдельный git-репозиторий с своим remote) сделан один
  локальный коммит (`551a799`, удаление устаревшего корневого
  `metadata.json`) — **не запушен**. Push туда — часть runbook B ниже.
- Дальше — Runbook A и Runbook B из `docs/agent-instructions-stage-2.md`.
  Это уже прод: бэкапы на сервере, импорт аннотаций в БД, миграция данных.
  **Останавливаюсь здесь и жду подтверждения пользователя перед тем, как
  что-либо деплоить.**
