# Test dependencies for RedPen.
#
# Core suite (pytest, runs without a browser): converter, sanitize, storage,
# API (via FastAPI TestClient), and build-layout tests.
pytest==7.3.1
fastapi==0.115.0
starlette==0.38.6
httpx>=0.27
jinja2~=3.0
Pillow>=8.0.0

# Браузерные сценарии в tests/manual/ (нужен установленный chromium):
# page_view_markers.py, offline_bundle_browser_test.py,
# editor_app_stand.py + editor_app_acceptance.py.
playwright==1.35.0
pytest-playwright==0.3.3
